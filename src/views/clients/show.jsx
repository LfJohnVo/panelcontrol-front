import React from 'react';
import { ViewLayout } from '../../components/common/layouts';

const ShowClient = () => {
  return (
    <ViewLayout>
      <div>test</div>
    </ViewLayout>
  );
};

export default ShowClient;
